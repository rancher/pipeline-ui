
import { dateDiff } from 'ui/helpers/date-diff';
import { module, test } from 'qunit';

module('Unit | Helper | date diff');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateDiff([42]);
  assert.ok(result);
});

