
import { pipelineFileExtension } from 'ui/helpers/pipeline-file-extension';
import { module, test } from 'qunit';

module('Unit | Helper | pipeline file extension');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pipelineFileExtension([42]);
  assert.ok(result);
});

